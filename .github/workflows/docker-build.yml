name: Build and Push Docker image

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v6
        with:
          context: "./"
          file: Dockerfile
          push: true
          tags: registry.luxembourg-2.cloud.gcore.dev/59508-197526-76-infra/vitacraft:${{ github.ref_name }}

  helm-chart:
    runs-on: ubuntu-latest
    needs: build
    env:
      CHART_VERSION: ${{ github.ref_name }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: 'latest'

      - name: Login to Harbor OCI registry
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | helm registry login ${{ vars.DOCKER_REGISTRY }} \
            --username ${{ secrets.DOCKER_USERNAME }} \
            --password-stdin

      - name: Package Helm chart
        run: |
          helm dependency update ci/helm/vitacraft
          helm package ci/helm/vitacraft \
            --version "${CHART_VERSION#v}" \
            --app-version "${CHART_VERSION}"

      - name: Push Helm chart to OCI registry
        run: helm push vitacraft-${CHART_VERSION#v}.tgz oci://${{ vars.DOCKER_REGISTRY }}/59508-197526-76-infra/charts
